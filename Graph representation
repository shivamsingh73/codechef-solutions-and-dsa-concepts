#include<bits/stdc++.h>
using namespace std;

int main(){
     int n,m;
     cin>>n>>m;
     
     int adj[n+1][m+1];
     
     
     for(int i=0;i<m;i++){
     int u,v;  // edge from vertex u to vertex v;
      cin>>u>>v;
      int adj[u][v] = 1
      
      // adjMat[v][u] = 1;
      
      
     }
     
    return 0;
}
